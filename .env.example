PORT=4000
NODE_ENV=development
DATABASE_URL="file:./dev.db"
TOKEN_KEY=change-me-to-a-long-random-secret
CORS_ORIGIN=http://localhost:3000
OTP_TTL_SECONDS=300
# true = OTP code shows in API response + server log (local only)
OTP_DEV_MODE=true
